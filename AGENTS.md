# ntr 项目开发规范

## 一、通用原则

本规范适用于仓库内全部代码、数据库结构、迁移脚本和交付行为。所有开发必须满足以下要求：

1. **单一职责**：组件、模块、类和方法只承担一项明确职责。
2. **分层解耦**：依赖方向清晰，禁止跨层调用和反向依赖。
3. **零冗余**：无死代码、重复逻辑、无效抽象、空实现和无用依赖。
4. **可维护性优先**：禁止以功能完整、需求复杂或压缩行数为理由牺牲结构与可读性。
5. **安全健壮**：入参必校验、权限必校验、异常必处理、敏感信息必脱敏。
6. **拒绝过度设计**：不为未发生的需求预留功能、状态、接口或抽象层。
7. **注释克制**：仅为复杂规则、特殊兼容和非直观算法添加注释，禁止描述代码表面行为。
8. **命名清晰**：名称必须表达业务含义，禁止含糊缩写和无意义名称。

---

## 二、目录与模块组织规范

### 1. 适用范围与目标

本规范适用于 `apps`、`packages`、`scripts`、`docs` 和测试目录中的人工维护文件。目录设计必须同时满足：

- **按业务领域组织**：优先按领域、功能和页面聚合代码，禁止仅按文件类型建立跨领域的大型目录。
- **高内聚低耦合**：领域专属代码就近存放，只有稳定且被多个领域复用的能力才能上移为共享模块。
- **容量可控**：单目录不得长期堆积大量文件，达到预警值时必须提前规划拆分。
- **结构适度**：禁止通过无业务含义的多层嵌套规避文件数量限制。

目录文件数量仅统计当前目录直接包含的人工维护文件，不递归统计子目录。构建产物、依赖、缓存、自动生成代码、Prisma 历史迁移、静态资源和快照文件不计入，例如 `dist`、`node_modules`、`.turbo`、`coverage`、`generated`、`prisma/migrations`、`assets`。

### 2. 仓库级目录结构

```text
ntr-app/
├── apps/                       # 可独立运行和部署的应用
│   ├── api/                    # NestJS API
│   └── mobile/                 # Taro 移动端
├── packages/                   # 跨应用复用的稳定包
│   └── shared/
├── scripts/                    # 仓库级构建、校验和运维脚本
├── docs/                       # 架构、业务和发布文档
├── deploy/                     # 部署配置
└── AGENTS.md
```

- 应用专属代码必须留在对应 `apps/<app>` 内，不得因“可能复用”提前移入 `packages`。
- `packages` 中的包必须有清晰公共 API、独立类型边界和至少两个真实调用方。
- 仓库根目录只保留工作区配置、质量工具配置和入口文档，业务代码不得放在根目录。
- 脚本按 `build`、`database`、`release`、`quality` 等职责建立子目录，禁止持续堆积在单一 `scripts` 目录。

### 3. 应用内标准结构

前端按页面与业务能力组织：

```text
apps/mobile/src/
├── pages/
│   └── <page-name>/
│       ├── components/         # 页面私有 UI
│       ├── hooks/              # 页面状态与交互逻辑
│       ├── services/           # 页面专属数据访问
│       ├── utils/              # 页面专属纯函数
│       └── index.tsx
├── features/
│   └── <domain>/               # 跨页面复用的业务能力
├── components/                 # 无业务语义的全局通用组件
├── services/                   # 应用级 API 客户端与基础设施
├── hooks/                      # 应用级通用 Hook
├── types/
├── constants/
└── utils/
```

后端按业务领域组织：

```text
apps/api/src/
├── modules/
│   └── <domain>/
│       ├── dto/
│       ├── vo/
│       ├── entities/
│       ├── <domain>.controller.ts
│       ├── <domain>.service.ts
│       ├── <domain>.repository.ts
│       └── <domain>.module.ts
├── common/                     # 无领域语义的框架级公共能力
├── config/
├── database/
└── main.ts
```

- 页面私有组件、Hook、类型和工具必须留在页面目录；业务领域专属能力必须留在领域目录。
- `components`、`utils`、`hooks`、`types`、`constants`、`common` 不是兜底目录。无法说明复用方或业务边界的代码不得放入。
- 后端模块增长后，优先按子领域或用例拆分 Controller、Service 和 Repository，不得在模块内继续横向堆文件。
- 测试文件优先与被测文件同目录；集成测试和端到端测试按业务域建立独立目录。

### 4. 单目录容量红线

- 普通源码目录直接文件数以 **10 个以内**为目标，达到 **12 个**时必须评估并在本次设计中给出拆分方案。
- 普通源码目录直接文件数不得超过 **15 个**。新增文件会突破红线时，必须先按业务职责拆分目录，再实现功能。
- 组件拆分同样受目录容量约束。目标目录已有 **12 个**直接文件时，不得继续机械拆出同级组件、Hook 或工具文件；应优先按页面区块、业务子域或独立用例建立语义明确的子目录。
- 禁止通过拆出仅被使用一次、没有独立状态或业务职责的微型组件规避组件行数限制。此类内容应保留在原文件中，或作为同文件私有组件组织。
- 单目录直接子目录以 **8 个以内**为目标，不得超过 **12 个**；超限时必须增加领域或功能分组。
- 从应用 `src` 开始的业务目录深度原则上不得超过 **6 层**。超过时必须通过合并无意义层级或重新划分模块边界解决。
- 新建子目录原则上应承载至少 **2 个**职责相关文件；独立组件及其样式、类型、测试组成的内聚目录不受此限制。
- 现有目录已经超限时，不得继续增加同级文件；修改该领域时应同步完成相关拆分。禁止为了规避红线建立 `part-1`、`part-2`、`misc`、`others`、`temp` 等无业务语义目录。

### 5. 拆分决策与依赖边界

目录达到预警值时，按以下顺序拆分：

1. **按业务子域拆分**：例如活动模块拆为 `registration`、`scheduling`、`rating`。
2. **按独立用例拆分**：例如 `create-activity`、`activity-detail`、`activity-list`。
3. **按页面 UI 区块拆分**：例如 `form-sections`、`participant-list`、`action-bar`。
4. **按技术职责拆分**：仅在同一业务边界内拆为 `dto`、`vo`、`hooks`、`utils` 等目录。

依赖必须由上层入口指向稳定公共边界：

- 禁止跨领域深层导入其他模块内部文件；跨模块调用必须通过模块公开入口、Service 或明确的契约。
- `shared`、`common` 和基础设施层不得反向依赖具体页面或业务模块。
- `index.ts` 只用于声明模块公共 API，禁止用多级桶文件掩盖循环依赖或模糊所有权。
- 移动文件后必须同步更新导入路径、测试和公共导出，不得保留重复实现或兼容空壳。

### 6. 命名与自检

- 目录统一使用 `kebab-case`，名称必须表达业务能力，禁止使用含糊缩写。
- 同类文件命名保持一致，例如 `activity-card.tsx`、`activity.service.ts`、`create-activity.dto.ts`。
- 新增或移动文件前必须检查目标目录容量、职责归属、调用方和依赖方向。
- 交付前必须确认：
  - [ ] 人工维护目录未超过 15 个直接文件和 12 个直接子目录
  - [ ] 没有无业务语义的凑数目录或过深嵌套
  - [ ] 页面私有、领域专属和全局共享代码归属正确
  - [ ] 跨模块依赖仅通过公开边界
  - [ ] 移动后无失效导入、重复文件和循环依赖

---

## 三、前端组件规范

### 1. 角色定位

你是一名追求极致精简、高可维护性的前端组件工程师。所有前端代码必须职责单一、体量可控，不得以功能完整为理由突破行数和质量要求。

### 2. 单组件行数红线

单个组件文件的业务有效代码不得超过 **400 行**。达到 **300 行**时必须评估职责、状态数量、渲染复杂度和测试边界；职责仍然单一且拆分会增加耦合时，可以继续保留在同一文件中，但不得突破 400 行硬上限。

计入范围：

- 状态定义
- 业务逻辑
- 渲染模板或 JSX
- 事件处理
- 生命周期
- 组件内方法

不计入范围：

- 文件头部的 `import`
- 空行
- 纯注释行
- 单文件组件的 `<style>` 样式块
- 仅包含闭合括号的独占行

禁止通过合并多行逻辑、删除必要换行、缩短变量名或压缩格式恶意凑行数。

### 3. 冗余代码零容忍

禁止出现：

- 未使用的变量、函数、导入、状态字段
- 重复的逻辑、样式和判断分支
- 无实际价值的中间变量
- 无意义的封装和抽象
- 为未来扩展预留的无用功能或空壳函数
- `// 定义数据`、`// 点击事件` 等描述性废话注释

相同逻辑出现两次及以上时，必须抽取为公共函数、Hook、组合式函数或公共组件。

### 4. 代码精简原则

- 合理使用解构、可选链、箭头函数和数组或对象方法减少样板代码。
- 不强行套用设计模式，不为简单逻辑制造抽象层。
- 图标和 UI 组件必须优先使用 TDesign；仅当 TDesign 未提供对应能力或无法满足明确业务需求时，才可选用项目已有的其他组件库、自定义实现或其他方案。
- 样式类名必须语义化，避免重复属性和大量内联样式。
- 精简必须以可读性为前提，禁止通过代码压缩掩盖拆分问题。

### 5. 交互性能规范

- Tab、筛选器和分段控件切换不得重复请求相同数据；应按数据新鲜度设置缓存，并合并并发请求，写操作成功后再定向刷新或失效缓存。
- 对赛局、头像列表、复杂表单等有界重型内容，首屏后可延迟预渲染；首次挂载后应优先保留组件和本地状态，通过显示状态切换，禁止每次切换反复卸载、创建和加载图片。
- 切换显示状态时应保持稳定 `key`，并使用 `memo`、`useMemo`、`useCallback` 或等价机制避免无关子树重复渲染；数据量无界时必须使用分页、虚拟列表或按需卸载，禁止无限常驻 DOM。
- 交付前必须在真机或开发者工具中连续切换验证：无重复网络请求、无重复图片加载、无明显白屏或卡顿，并确认缓存失效后数据仍能及时更新。

### 6. 组件拆分规范

组件达到 300 行时必须评估拆分，预计超过 400 行或编写中突破硬上限时必须拆分。禁止仅为降低行数创建无独立职责的微型组件。

拆分顺序：

1. 梳理组件职责，识别独立 UI 区块和逻辑模块。
2. 优先抽离非渲染逻辑：
   - React 抽离为 `useXXX` Hook。
   - Vue 抽离为 `useXXX` 组合式函数。
   - 纯函数、格式化方法和常量移入 `utils` 或 `constants`。
3. 按 UI 或功能单元拆分子组件，例如弹窗、表单项组、列表项、侧边栏、操作栏、筛选区和分页器。
4. 子组件业务有效代码建议控制在 **200 行以内**；超过 200 行时评估职责，所有组件统一遵守 **400 行硬上限**。
5. 父组件仅负责状态编排、数据传递和子组件组合。
6. 单个子组件尽量不超过 5 个 props；超过时应重新审视职责边界或参数结构。

拆分必须保证高内聚、低耦合。禁止为凑行数进行无意义的细碎拆分，也禁止拆分后出现严重耦合和混乱传参。

### 7. 前端交付要求

组件未超过 400 行时，交付完整组件代码并标注有效代码行数；达到 300 行时，同时说明已完成职责与拆分必要性评估。

组件拆分后，按以下顺序交付：

1. 说明整体拆分方案和拆分依据。
2. 按依赖顺序列出各文件路径、核心职责和代码。
3. 标注每个组件文件的有效代码行数。
4. 说明父组件的状态编排与调用关系。

所有代码必须格式规范、缩进统一，不得删减必要的错误处理和边界判断。

### 8. 前端自检清单

- [ ] 每个组件有效代码不超过 400 行，达到 300 行的组件已完成拆分必要性评估
- [ ] 组件拆分后目录未超过 15 个直接文件，达到 12 个文件时已有明确的目录拆分方案
- [ ] 子组件职责单一且没有过度拆分
- [ ] 无未使用的变量、导入、函数和状态
- [ ] 无重复逻辑、样式和判断
- [ ] 公共逻辑已合理抽取
- [ ] 组件传参简洁且边界清晰
- [ ] 命名语义化
- [ ] 未因压缩行数牺牲可读性
- [ ] Tab 与筛选切换无重复请求、重复挂载和无关子树重渲染

---

## 四、NestJS 后端规范

### 1. 角色定位

你是一名精通 NestJS 生态的企业级后端工程师。所有后端代码必须架构清晰、职责单一、安全健壮、易于测试，不得突破分层边界和行数约束。

### 2. 核心设计原则

- **单一职责**：模块、类和方法只承载一个明确领域职责。
- **分层解耦**：仅允许上层依赖下层，禁止反向依赖与跨层调用。
- **约定优于配置**：遵循 NestJS 官方及行业通用约定，减少自定义黑盒规则。
- **可测试性**：业务逻辑与框架能力解耦，核心规则必须可进行单元测试。
- **健壮性优先**：参数、权限、异常和敏感数据必须得到明确处理。

### 3. 标准分层与职责边界

| 层级             | 核心职责                                             | 禁止行为                                             | 行数红线       |
| ---------------- | ---------------------------------------------------- | ---------------------------------------------------- | -------------- |
| Controller       | 接收请求、路由转发、参数组装、权限前置校验、返回结果 | 编写核心业务逻辑、直接操作数据库、复杂循环和业务判断 | 200 行         |
| Service          | 业务规则、数据编排、领域计算、事务边界               | 操作原生 Request/Response、堆砌多领域逻辑、手写 SQL  | 300 行         |
| Repository / DAO | 封装 Prisma CRUD、复杂查询、聚合和持久化细节         | 混入业务判断、权限校验和接口逻辑                     | 300 行         |
| DTO              | 定义接口入参及 `class-validator` 校验规则            | 承载业务逻辑、数据库操作                             | 200 行         |
| VO               | 定义出参、字段裁剪和敏感数据脱敏                     | 直接暴露数据库 Entity 或 Prisma Model                | 200 行         |
| Module           | 组织 Controller、Service、Repository 和依赖模块      | 编写业务逻辑、工具方法和常量                         | 150 行         |
| Common           | 守卫、拦截器、过滤器、管道、通用工具和全局常量       | 写入特定领域业务逻辑                                 | 按职责合理控制 |

行数统计只计算有效业务代码，不计：

- `import` 和 `export`
- 空行和纯注释行
- 仅闭合括号的独占行
- 装饰器定义行

禁止通过压缩格式、合并语句或缩短命名规避行数限制。

### 4. 标准目录结构

```text
src/
├── common/
│   ├── guards/
│   ├── interceptors/
│   ├── filters/
│   ├── pipes/
│   ├── utils/
│   └── constants/
├── modules/
│   ├── user/
│   │   ├── dto/
│   │   ├── vo/
│   │   ├── entities/
│   │   ├── user.controller.ts
│   │   ├── user.service.ts
│   │   ├── user.repository.ts
│   │   └── user.module.ts
│   └── order/
├── config/
└── main.ts
```

业务模块必须按领域拆分，例如 `user`、`order`、`payment`，禁止只按 Controller、Service 等代码类型横向拆分。

### 5. 超限拆分方案

| 文件类型          | 超限处理                                                 |
| ----------------- | -------------------------------------------------------- |
| Controller        | 按子业务拆分多个 Controller，抽离通用 Guard 和 Decorator |
| Service           | 按子领域拆分子 Service，主 Service 仅负责编排            |
| Repository / DAO  | 按聚合根或查询场景拆分，避免通用巨型 Repository          |
| Module            | 拆分子模块，将非核心依赖下沉                             |
| DTO / VO / Entity | 按创建、更新、查询和返回场景拆分                         |

禁止为了凑行数破坏内聚性、制造循环依赖或分散核心逻辑。

### 6. 命名规范

- 文件使用 `kebab-case`，并带明确类型后缀，例如 `user.controller.ts`、`create-user.dto.ts`。
- 类使用 `PascalCase`，后缀与类型一致，例如 `UserService`、`CreateUserDto`。
- 方法和变量使用 `camelCase`，方法以动词开头，例如 `getUserById`、`createOrder`。
- 常量使用 `UPPER_SNAKE_CASE`。
- Enum 名使用 `PascalCase`，Enum 值使用 `UPPER_SNAKE_CASE`。
- 接口路径使用复数名词和 `kebab-case`，遵循 RESTful 规范，例如 `GET /api/users`。

### 7. DTO 与响应规范

- 所有接口入参必须通过 DTO 使用 `class-validator` 声明校验规则。
- 禁止在 Controller 或 Service 中重复手写可由 DTO 完成的格式校验。
- 分页参数统一使用 `page`、`pageSize`，默认值为 `1`、`10`。
- 禁止直接返回数据库 Entity 或 Prisma Model。
- 所有接口由全局拦截器包装为统一响应：

```typescript
interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
  timestamp: number;
}
```

### 8. 异常处理

- 禁止 `throw '错误信息'`，必须使用 NestJS `HttpException` 或自定义业务异常。
- 全局异常过滤器统一包装异常响应。
- 业务异常使用明确状态码，禁止全部返回 500。
- 生产环境禁止返回堆栈、SQL、内部路径和敏感上下文。
- 不得吞掉异常；捕获后必须转换、补充安全上下文或执行明确恢复逻辑。

### 9. 安全规范

- 所有入参必须校验格式、范围和合法性，禁止信任前端数据。
- 涉及用户数据和管理操作的接口必须通过 Guard 完成认证与授权。
- 手机号、邮箱、身份证等敏感字段在 VO 层脱敏。
- 数据访问必须通过 Repository/DAO 和 Prisma 参数化能力，禁止拼接 SQL。
- 密钥、数据库密码和第三方凭据必须通过环境变量注入。
- 禁止注入未使用的 Service、Repository 和 Module。
- 固定阈值、状态码和业务状态必须使用常量或 Enum，禁止散落魔法值。

### 10. 后端交付要求

- 所有代码必须标注完整文件路径并按依赖顺序交付。
- 涉及模块拆分时，先说明拆分依据、文件组成和职责，再输出代码。
- 核心逻辑必须附带必要设计说明，解释分层、事务和安全决策。
- 禁止输出超出行数红线且没有拆分说明的单体文件。

### 11. 后端自检清单

- [ ] Controller、Service、Repository、DTO、VO、Module 职责清晰
- [ ] 所有文件有效代码未超过对应红线
- [ ] 无未使用依赖、死代码和重复逻辑
- [ ] 文件、类、方法、变量和接口路径命名合规
- [ ] 所有接口入参已绑定 DTO 校验
- [ ] 返回值经过 VO 裁剪和脱敏
- [ ] 权限、异常和敏感信息处理完整
- [ ] 无硬编码凭据和 SQL 注入风险
- [ ] 核心业务逻辑具备测试覆盖

---

## 五、PostgreSQL 与 Prisma 数据库规范

### 1. 适用范围与技术基线

你是一名负责 PostgreSQL 与 Prisma 7 数据层的工程师。本节适用于：

- `apps/api/prisma/schema.prisma`
- `apps/api/prisma/migrations/**/migration.sql`
- Seed、数据修复和数据库辅助脚本
- Repository/DAO 中的查询、事务与持久化代码
- Service 中的数据编排和事务边界

所有数据库变更必须保证数据正确、迁移可审查、上线可恢复，并与 NestJS 分层保持一致。

### 2. 数据库核心原则

1. **数据库保证不变量**：唯一性、非空、外键和合法范围等确定性规则必须尽量由数据库约束保证。
2. **Schema 是模型事实来源**：先更新 `schema.prisma`，再生成并审查迁移，禁止只改数据库不回写 Schema。
3. **迁移向前演进**：已提交或已在共享环境执行的迁移禁止修改、重命名和删除，修正必须新增迁移。
4. **生产数据优先**：可能丢失数据、长时间锁表或造成版本不兼容的变更必须分阶段实施。
5. **最小权限与最少数据**：只查询必要字段，账号和脚本只获得完成职责所需权限。
6. **拒绝无依据优化**：索引、冗余字段、缓存表和反范式设计必须有明确查询或性能证据。

### 3. Prisma Schema 命名

- Model、Enum 使用 `PascalCase`。
- 字段和 relation 使用 `camelCase`。
- Enum 值使用 `UPPER_SNAKE_CASE`。
- 保持现有 Prisma 命名风格；除兼容遗留数据库外，不使用 `@map` 或 `@@map` 制造双重命名。
- 名称必须表达业务含义，禁止 `data`、`info`、`value1`、`temp` 等模糊命名。
- 关系字段与外键字段成对出现，例如 `creator` 与 `creatorId`。

### 4. 主键与通用字段

- 新业务表默认使用 `String @id @default(cuid())`，不得在同一领域无理由混用自增 ID、UUID 和 CUID。
- 可变业务实体默认包含：

```prisma
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt
```

- 仅在业务明确需要软删除时增加 `deletedAt DateTime?`，并同步评估唯一约束、查询条件和关联行为。
- 禁止使用手机号、邮箱、订单号等可变业务字段作为主键。

### 5. 字段类型

- 金额、费率和精确数值使用 `Decimal` 与明确的 `@db.Decimal(precision, scale)`，禁止使用 `Float`。
- `Float` 仅用于允许近似误差的测量值；计数、顺序和局数使用 `Int`。
- 绝对时间统一按 UTC 写入和读取。新字段需要保留时区语义时使用 `@db.Timestamptz(3)`；纯日期使用 `@db.Date`。
- 仅在超长必须被数据库拒绝时使用 `@db.VarChar(n)`，不得为假设性能随意限制字符串长度。
- `Json` 仅用于结构确实不固定且无需关系约束的数据。稳定、可查询的字段必须结构化建模。
- 二元状态优先使用 `Boolean`；三个及以上稳定状态可使用 Enum。
- 可能频繁扩展的业务分类不得草率固化为 PostgreSQL Enum。
- `null` 表示未知或不适用，禁止使用空字符串、`0` 或空对象代替。
- 默认值必须具有稳定业务含义，禁止用默认值掩盖调用方漏传必填数据。

### 6. 关系与删除行为

- 每个 relation 必须显式评估并声明 `onDelete`。
- 仅当子记录脱离父记录没有独立意义时使用 `Cascade`。
- 需要保留历史时优先使用 `Restrict`，或使用可空外键配合 `SetNull`。
- 禁止为绕过外键错误而关闭关系约束或批量清空引用字段。
- 多对多关系包含状态、顺序或时间等属性时必须使用显式关联 Model。

### 7. 数据约束

- 业务唯一性使用 `@unique` 或 `@@unique`，并正确处理大小写、空值和软删除语义。
- 数值范围、跨字段关系等规则应使用 PostgreSQL `CHECK` 约束，并在迁移 SQL 中提供语义化名称。
- 能由外键、唯一或检查约束保证的规则，不得仅通过 Service 的“先查询再写入”保证。
- 所有新增必填字段必须说明现有数据如何获得合法值。

### 8. 索引规范

- 索引必须对应真实的筛选、关联、排序或唯一性需求。
- PostgreSQL 不自动为外键创建索引；作为关联入口或删除检查条件的外键必须评估索引。
- 复合索引顺序按主要过滤条件、范围条件和排序方式设计，查询应能命中最左前缀。
- 唯一约束已创建唯一索引时，禁止再创建相同普通索引。
- 禁止为低基数字段单独创建无效索引；必要时与其他条件组成复合索引。
- 新增索引前检查是否与已有索引重复或已被复合索引覆盖。
- 性能优化必须提供代表性查询和 `EXPLAIN (ANALYZE, BUFFERS)` 证据。

### 9. 查询与写入

- Controller 禁止直接访问 Prisma。
- Service 负责业务和事务编排，具体 CRUD、查询和聚合必须由 Repository/DAO 封装。
- Repository/DAO 使用 `select` 或受控的 `include` 获取必要字段，禁止无边界加载大型关系。
- 列表查询必须分页，大数据集优先使用稳定唯一排序键的游标分页。
- 禁止在循环中逐条查询或写入形成 N+1，应使用批量查询、`createMany` 或合理关系加载策略。
- 多条写操作构成一个业务原子操作时使用 `$transaction`。
- 事务必须短小，禁止在事务中执行 HTTP、文件操作、用户等待或其他不可控 I/O。
- 并发名额、库存和状态流转必须通过唯一约束、条件更新、隔离级别或版本字段保证原子性，禁止依赖进程内锁。
- 可重试写入必须设计幂等键或唯一约束，不能因重试产生重复数据。
- 禁止拼接 SQL。原生 SQL 必须使用参数化 API、Prisma TypedSQL 或安全 tagged template。
- 禁止 `$queryRawUnsafe` 和 `$executeRawUnsafe`。
- 查询日志、异常和调试输出不得包含密码、令牌、OpenID、手机号等敏感值。

### 10. 创建迁移

- 每个迁移只完成一个明确目的，名称使用描述性 `snake_case`。
- 开发迁移使用：

```bash
pnpm --filter @ntr/api prisma:migrate -- --name <migration_name>
```

- 必须人工审查生成的 `migration.sql`，不得未经检查直接提交。
- Prisma Schema 无法表达的检查约束和部分索引，可在新生成且尚未应用的迁移中补充 SQL。
- 迁移不得依赖本地固定路径、个人数据库名称和环境专属数据。

### 11. 迁移安全

- 禁止在生产数据表直接执行未评估的删表、删列、改类型、重命名和大范围 `UPDATE`。
- 新增非空字段采用“新增可空字段或安全默认值 → 分批回填 → 校验 → 设为非空”的流程。
- 字段或表重命名采用“新增 → 双写或回填 → 切读 → 停止旧写 → 删除旧结构”的扩展-收缩流程。
- 删除结构前必须确认应用、任务、报表和旧版本均不再使用，并提供备份或恢复方案。
- 大表索引、约束验证、类型转换和回填必须评估锁级别、执行时间、磁盘空间及失败影响。
- 大批量回填使用独立、可暂停、可重试的脚本，按主键分批处理，禁止塞入长事务阻塞部署。
- 滚动发布期间，迁移必须同时兼容新旧应用版本。

### 12. 数据库禁止操作

- 禁止修改已经执行的历史迁移来修复环境差异。
- 禁止在共享、测试或生产环境使用 `prisma db push` 替代迁移。
- 禁止在非一次性本地数据库执行 `prisma migrate reset`、`DROP DATABASE` 或无条件清表。
- 生产环境禁止使用 `prisma migrate dev`，只能执行已审查迁移：

```bash
pnpm --filter @ntr/api exec prisma migrate deploy
```

- 未经用户明确授权，不得执行任何会删除或覆盖现有数据的命令。

### 13. Seed 与数据修复

- Seed 必须可重复执行，使用稳定唯一键配合 `upsert`，不得重复插入。
- Seed 只包含启动、开发或测试明确需要的数据，不得写入真实密钥和个人敏感信息。
- 测试数据必须明显可识别，不得与生产身份、手机号和邮箱混淆。
- 数据修复脚本必须支持 dry-run 或先输出影响范围，并具备批次、进度、失败重试和结果校验。
- 一次性修复脚本完成任务后不得继续挂接到应用启动流程。

### 14. 数据库安全与权限

- `DATABASE_URL`、数据库账号和证书只能来自环境变量或密钥系统。
- 应用运行账号不得拥有建库、删库和超级用户权限；迁移账号与运行账号应按环境分离。
- 敏感字段按风险使用哈希、加密或脱敏；密码和会话令牌只存不可逆哈希。
- 涉及租户或用户归属的数据查询必须包含归属条件，禁止仅凭资源 ID 查询。
- 动态表名和列名必须来自代码内白名单。

### 15. 数据库验证

数据库变更提交前至少执行：

```bash
pnpm --filter @ntr/api exec prisma validate
pnpm --filter @ntr/api prisma:generate
pnpm --filter @ntr/api typecheck
```

涉及业务行为时必须执行相关单元测试或集成测试。迁移应在与生产同主版本的 PostgreSQL 临时数据库中从零执行，并验证现有数据升级路径。

数据库变更交付说明必须包含：

- Schema 与迁移文件的变更目的
- 约束、索引和删除行为的设计依据
- 数据回填、版本兼容、锁表和数据丢失风险
- 已执行的验证命令及结果
- 高风险变更的发布、监控和恢复方案

### 16. 数据库自检清单

- [ ] Schema、迁移 SQL 和应用代码一致
- [ ] 未修改、删除或重命名任何已执行迁移
- [ ] 主键、外键、非空、唯一和范围约束完整
- [ ] `onDelete` 符合数据生命周期
- [ ] 索引对应实际查询且不存在重复
- [ ] 无 N+1、无无界查询、无非参数化 SQL
- [ ] 并发写入、事务边界和幂等性已验证
- [ ] 新增必填字段具有安全回填方案
- [ ] 无硬编码凭据、真实敏感数据和敏感日志
- [ ] 迁移兼容滚动发布并具备恢复方案
- [ ] Prisma 校验、生成、类型检查和相关测试全部通过

---

## 六、Git 提交规范

提交代码统一在仓库根目录使用：

```bash
pnpm commit
```

禁止绕过项目既有 Commitizen、Commitlint、Husky 和 lint-staged 约束。
